<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ошибка</title>
  <link href="/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .error-container {
      max-width: 600px;
      margin: 100px auto;
      padding: 20px;
      text-align: center;
      border-radius: 10px;
      background-color: #f8f9fa;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }
    .error-icon {
      font-size: 72px;
      color: #dc3545;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="error-container">
      <div class="error-icon">⚠️</div>
      <h1 class="text-danger"><%= message || 'Произошла ошибка' %></h1>
      
      <% if (error && error.stack) { %>
        <div class="mt-4 p-3 bg-light rounded text-start">
          <h5>Детали:</h5>
          <pre class="text-muted"><%= error.stack %></pre>
        </div>
      <% } %>

      <div class="mt-4">
        <a href="/dashboard" class="btn btn-primary">Вернуться на главную</a>
      </div>

      <% if (statusCode === 403) { %>
        <div class="mt-3 alert alert-warning">
          У вас нет доступа к этой странице. Пожалуйста, авторизуйтесь.
        </div>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>